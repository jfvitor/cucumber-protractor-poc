"use strict";
var util = require("util");
function filename(artifact) {
    var extension = artifact.extension || "jar";
    if (artifact.classifier) {
        return util.format("%s-%s-%s.%s", artifact.artifactId, artifact.version, artifact.classifier, extension);
    }
    ;
    return util.format("%s-%s.%s", artifact.artifactId, artifact.version, extension);
}
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = filename;
//# sourceMappingURL=filename.js.map