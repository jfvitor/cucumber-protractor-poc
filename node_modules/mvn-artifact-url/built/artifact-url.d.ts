export interface Artifact {
    groupId: string;
    artifactId: string;
    version: string;
    extension?: string;
    classifier?: string;
}
export default function artifactUrl(artifact: Artifact, basepath: string): string;
