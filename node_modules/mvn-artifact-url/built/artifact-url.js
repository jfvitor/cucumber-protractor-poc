"use strict";
var path = require("path");
var mvn_artifact_filename_1 = require("mvn-artifact-filename");
var groupPath = function (artifact) {
    return [artifact.groupId.replace(/\./g, "/"),
        artifact.artifactId,
        artifact.version
    ].join("/");
};
var artifactPath = function (artifact) {
    return path.join(groupPath(artifact), mvn_artifact_filename_1.default(artifact));
};
function artifactUrl(artifact, basepath) {
    var prefix = basepath || "https://repo1.maven.org/maven2/";
    return prefix + artifactPath(artifact);
}
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = artifactUrl;
//# sourceMappingURL=artifact-url.js.map